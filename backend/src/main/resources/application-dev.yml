app:
  auth:
    enabled: false
  jwt:
    secret: stopforfuel-dev-secret-key-min-32-chars!!
  seed-test-data:
    enabled: true

spring:
  datasource:
    password: ${DATABASE_PASSWORD:myPassword}
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
